image: python:3.8

before_script:
  - pip install jupyter jupyterlab
  - echo $PATH
  - which jupyter

pages:
  script:
    - jupyter nbconvert --to markdown assets/jupyter/blog.ipynb
    - bundle install
    - bundle exec jekyll build -d public
  artifacts:
    paths:
      - public
  only:
    - main